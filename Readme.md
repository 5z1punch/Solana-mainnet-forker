# Haha, It's stillborn. Because his mother, Solana is dying. 

# That's a lesson, keep away from a "to CV" public chain. 

## manual
```bash
cd snapshoot && \
solana -u m account --output json --output-file {pubkey}.json {pubkey}
```
